# 개인 프로젝트 로드맵

## 현재까지 진행 상황 (2026-07-28 기준)

- 07/13 (월): LangChain 마이그레이션 완료
- 07/15 (수): LangGraph 마이그레이션 완료
- LangSmith 추적 연동 완료
- 데이터 품질 개선: 줄거리 보강 (498/500), 감독 정보 추가 (500/500)
- 감독 검색 구현 (info 청크 + 다국어 임베딩)
- 장르 검색 개선 (LangGraph genre 노드 - 조건 검색 분리)
- FastAPI 백엔드 + HTML 프론트엔드 (ASKFLIX) 연결
- 응답 스트리밍 구현 (LangGraph stream -> FastAPI -> 프론트)
- Docker 컨테이너화 (Dockerfile + docker-compose, 프론트까지 단일 컨테이너로 통합)

## 현재 문제점 및 개선 방향

### 1. 스포일러 판단 방식이 버전마다 다르고, 모두 아쉬움
- **바닐라 RAG**: 키워드(결말/스포/줄거리) 유무로 판단 -> "스포 없이"처럼 부정 표현을 잘못 판단할 수 있음
- **LangChain**: 검색 필터 없이 프롬프트 규칙으로 LLM이 자제 -> 100% 보장되지 않음
- **LangGraph**: 분류 노드에서 LLM(제미나이)이 판단 -> 정확하지만 질문 1건당 API를 2번씩 호출해서 무료 한도 소진이 빠름

### 2. 검색 경로에 따른 결과 편차
- 장르 노드로 가면 항상 상위 5개가 결과로 나오지만, 벡터 검색으로 가면 LLM 판단에 따라 결과 개수가 들쭉날쭉함
- 장르 감지가 키워드 기반이라 "무서운 영화" 같이 유사 표현은 잘 못 잡음

### 3. 감독 검색이 임베딩에 의존
- 감독명 (영어) <-> 질문 (한글) 매칭을 다국어 임베딩에 의존 -> 유명 감독은 잘 되지만 마이너 감독은 약할 수 있음

### 개선 목표
- 스포일러 유무를 판단하는 분류 모델을 직접 구현해서 API 호출 없이 판단 -> 정확도 확보 및 API 요청 수 절감
- LangSmith Dataset 기반 정량 평가로 검색, 답변 품질을 수치화하기

## 일정

> 07/16 ~ 07/20 진행 X, 멘토링 전 남은 이틀 (07/21 ~ 07/22) 기준
> 강사님 우선순위 (LangGraph -> 배포 -> 개인모델)에 따라
> LangGraph는 현재 상태로 일단 마무리하고 배포를 우선 진행 (11주차, 10주차 -> 9주차 순서)
> 배포 시 확인용 화면이 필요하므로 Streamlit 프론트도 함께 만들기

### 07/21 (화) - 서버 + 프론트
- [✅] FastAPI로 챗봇을 REST API로 래핑
- [✅] Streamlit으로 간단한 웹 프론트엔드 제작
- [✅] 프론트 -> FastAPI 연동해서 웹에서 대화 확인 (CLI -> 웹)

### 07/28 (화) - 컨테이너 + 배포
- [✅] Dockerfile 작성 -> 로컬에서 컨테이너로 실행
- [✅] docker-compose로 FastAPI + Streamlit 함께 실행
- [✅] (여유 있다면) AWS EC2 배포 시도

### 07/29 (수) - 배포 마무리
- [✅] AWS EC2 인스턴스 생성 + 키페어 발급
- [✅] EC2 접속(SSH) -> Docker 설치 -> git clone -> .env 생성
- [✅] docker compose로 실행 + 8000 포트 개방 -> 외부 접속 확인

### 이후 계획
- [] LangSmith Dataset 기반 정량 평가 (데이터셋 구성 -> 지표 정의 -> 측정)
- [] 스포일러 분류 모델 직접 구현
- [] 기능 추가: OTT 시청 정보 (TMDB watch providers), 포스터 이미지, 장르 감지 개선
- [] UI 개편: PC 화면 버전으로 만들기 + 넷플릭스 오마주
